# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts.tsbuildinfo
*.tsbuildinfo

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# sources brutes des assets 3D (non compressées, plusieurs dizaines de Mo) —
# seule la version optimisée dans public/models/ est versionnée et servie
/assets/

# outillage local (agents IA, éditeurs) — jamais versionné, même
# convention que Bobine (frontend/.gitignore)
.claude/
.agents/
.agent/
.gemini/
CLAUDE.md
AGENTS.md
.vscode/
.idea/
